name: Percy PDF Visual Regression

on:
  workflow_dispatch:

jobs:
  percy-pdf-test:
    runs-on: ubuntu-latest

    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v3

      - name: 🟢 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📥 Download PDF.js dist zip
        run: |
          curl -L -o pdfjs-3.4.120-dist.zip https://github.com/mozilla/pdf.js/releases/download/v3.4.120/pdfjs-3.4.120-dist.zip

      - name: 📂 Extract PDF.js dist and wait
        run: |
          npx extract-zip ./pdfjs-3.4.120-dist.zip
          echo "Waiting 5 seconds after extraction..."
          sleep 5

      - name: 📦 Install dependencies
        run: npm install

      - name: 🧪 Run Percy PDF Comparison (with built-in HTTP server)
        run: |
          echo "Waiting 5 seconds before running test..."
          sleep 5
          npm test -- configs/getting-started-scenarios/01_pdf-docs-run-info-baseline.yml
